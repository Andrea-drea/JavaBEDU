// Definicion de una interfaz llamada Autenticable
public interface Autenticable {
    //Metodo abstracto que debe ser implementado por las clases que implementen
    //Su propósito es realizar un proceso de autenticación
    boolean autenticar();
}
